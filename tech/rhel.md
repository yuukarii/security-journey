# RHEL

## RPM
```bash
# Install rpm package
rpm -i package.rpm

# Upgrade rpm package
rpm -U package.rpm

# List all installed packages
rpm -qa

# Querying rpm package
rpm -q package_name

# Verify without installing
rpm -V package_name

# Uninstall rpm package
rpm -e package_name
```